# ILs-screening-tm

An interactive screening tool for ionic liquid cation generation, structural filtering, synthetic accessibility scoring, and melting point ($T_m$) prediction.

## Installation

```bash
pip install .

